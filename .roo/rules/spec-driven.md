# Mandatory spec-driven workflow

Read and follow `/AGENTS.md` before changing implementation. Do not implement a behavioral change without an approved spec package under `/specs`. Keep requirements, plan, and tasks synchronized with the code, and run the spec guard before completion.
